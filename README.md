# rust-roguelike

This is a simple roguelike, written in Rust using Specs and RLTK.

To run it:
`cargo run`

The controls are:
Up/W = Move Up
Left/A = Move Left
Right/D = Move Right
Down/S = Move Down

G = Pickup Item
I = Access Inventory

To attack enemies, walk into them.
